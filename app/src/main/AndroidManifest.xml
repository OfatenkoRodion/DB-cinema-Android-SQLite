<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="best.the.rodionofatenko.com.Main">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="best.the.rodionofatenko.com.Main.AddSessionActivity" />
        <activity android:name="best.the.rodionofatenko.com.Main.BasicActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="best.the.rodionofatenko.com.Main.FilmActivity" />
        <activity android:name="best.the.rodionofatenko.com.Main.HallActivity"></activity>
    </application>

</manifest>